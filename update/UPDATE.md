## 更新
1. 2026.4.18
   - [x] 项目管理抽屉性能优化：任务区与属性区默认折叠，首屏优先展示标签预览（支持换行）
   - [x] 项目管理折叠态视觉优化：任务/属性标签采用当前任务主题色低透明底板，提升扫描效率
   - [x] 属性列表改为按需加载：打开项目管理不再立即拉取属性，展开“属性”区后再加载，减少卡顿
   - [x] 项目管理长列表稳态优化：展开态任务/属性列表加入滚动容器（max-height），避免表单被挤压下移
   - [x] 术语统一清理：Todos 改为“待办中心”，Time 页 `isOverview` 语义替换为 `isGlobalScope`
   - [x] 落实 `update4.md` Phase 1：导航改为 `Records / Todos / Time / Stats`，默认进入 Records，移除顶部任务下拉与 Overview
   - [ ] 属性添加还是有细节问题，步长的设置（是添加属性的最后一个输入框吗？）还是把属性添加复杂化了，目标和权重依旧只有输入框，但是不知道在输入什么的问题（得给个标识），然后输入模式也不能太麻烦了，也就两个模式，一个签到，一个计数，只需要换成一个仅签到的checkbox就行了，仅签到就把目标和权重隐藏起来
   - [x] 上线“今日指挥中心（Command Center）”：跨任务属性卡片 + 任务 Tag 多选筛选 + 统一保存
   - [x] 新增 Inbox v1 聚合区：承载 `pending_settlement` 与 `critical_error`，支持 `Renew / Evolve / Archive`
   - [x] 任务与属性管理下沉到“项目管理”侧边抽屉，主流程不再依赖页面级任务容器
   - [x] 新增后端聚合接口 `GET /v1/home/command-center`（任务、筛选、inbox、记录卡片、todo/focus 摘要）
   - [x] 新增后端批量记录接口 `PUT /v1/home/records/{record_date}`，支持 `value=null` 撤销当日记录
   - [x] Time/Stats 新增页内筛选（全局/单任务），默认全局，任务分析按页内切换
   - [x] 属性编辑器接入 `ux_config`：`input_mode`、`quick_step`、`detail_enabled` 写入 `calc_config.schedule_config`
   - [x] 兼容策略落地：`task_id=1` 继续保留但对用户隐藏，不做 schema 迁移
   - [x] 最小校验通过：`frontend build + test` 与 `backend pytest` 全绿
   - [x] 落实 `update3.md`：引入过期属性虚拟状态 `pending_review`（前端基于 `period_end` + 当前日期判断）
   - [x] Records 待结算拦截：过期属性置顶高亮并屏蔽常规输入，改为 `🎁 周期结束，点击结算`
   - [x] 新增结算弹窗工作流：`Renew`（按历史周期天数续期）/ `Evolve`（直接进入属性编辑）/ `Archive`（标记归档）
   - [x] 归档策略落地：`attr_sign=2`，不再参与 Records 日常录入；历史数据保留用于统计
   - [x] Stats 新增“🎓 过往里程碑 (Archived Goals)”只读区域，展示归档属性生命周期结果
   - [x] 补充时间规划边界稳定性：新增单测覆盖“仅开始/仅结束/非法区间”三类 `period_start/period_end` 情况
   - [x] 待结算卡片周期展示占位统一为“不限 ~ 不限”，与“默认不设限”文案一致

2. 2026.4.17
   - [x] 高级属性统计与显示优化（在`update2.md`指导下完成推进）
   - [x] 任务属性管理界面小字不能正确反应增加时间规则的任务现状，不开启时间规则用现有显示逻辑，开启时间规则就按照时间规则的“当前设定：...”显示
   - [x] 周折线图x坐标改为完全显示（月保持不变）
   - [x] record界面点击修改以后保存提示今日已签到，请优化“重复签到过滤机制”，至少不能把数据修改给拦截掉
   - [x] 对今天以后对的属性进行签到时发现无法签到但没有任何提示，当第二次进行修改会提示“今日已签到，无法重复签到”，十分不合理，顺应上一条更新任务优化“重复签到”机制，并且用户进行修改时应该提示“无法为未来签到”
   - [x] 添加属性界面点击设置目标值后的权重只给了一个输入框，但是没有任何标识和说明，可以把目标值输入框和权重输入框都加上标题，目标输入框添加名称为“每日（无时间规划）目标值”下面加上小字（说明文字）：“不开启时间规划时默认需要每日完成，填入每天要达成的目标，若要自定义请打开时间规划”；权重输入框添加名称“权重”，说明文字：“将按照该属性完成度*权重占总体权重比值计算每日任务完成度”
   - [x] 展开属性时间规划后具体哪天什么规则都按照添加规则来，所以优化这些个问题：
       1. [x] 设置目标和添加时间规则绑定，只有点击设置目标才可以添加规则，需要给文字说明；
       2. [x] 不开启目标设置时的时间规划只需要给一个周图，然后让用户选择在哪几天生效，不需要目标值和其他任何东西，注意不要放过一天都没选择的属性创建，顺便检查后端是否能处理这种规则化的无目标属性
       3. [x] 而且展开时间规划设置后原来的目标失效也要给视觉反馈 
       4. [x] 删除掉“其余日期默认（daily）”，再次重申时间规划只需要添加规则，我们尽量不搞逻辑复杂，我们要大道至简，而且这个功能是有bug的，直接砍掉就行

   - [x] 修改保存也应该直接保存并直接跳转“今日成就”
   - [x] 任务修改，可以复用创建界面进行修改
   - [x] 属性修改，依然复用创建界面
   - [x] 性能优化（阶段）：任务作用域刷新改为按页签动态记录查询窗口（默认轻量窗口；Records/Stats 扩展窗口），减少日常操作的无效历史记录拉取
   - [x] 桌面壳补齐 `update_task` / `update_task_attr` Rust bridge command，`tauri` 模式可用同一编辑链路
   - [x] Records 配置抽屉开启动画卡顿优化：抽屉先打开，重内容延迟两帧挂载，并去除遮罩模糊

3. 2026.4.16
   - [x] 按 `update1.md` 重构高级周期覆盖 UI：新增“开启高级周期覆盖”开关 + 规则卡片（星期多选/独立结算/共享目标/目标值）
   - [x] 覆盖规则冲突控制：同一星期在多规则中互斥（已占用星期置灰并提示来源规则）
   - [x] 覆盖实时摘要上线：展示“共享/独立/daily 默认”组合预览文案
   - [x] 完全移除“连接/已连”交互与旧小周期输入区域
   - [x] 完成度算法口径统一：`Overview / Records / Stats` 全部切换到 schedule-aware 计算（支持 `daily / specific_days / shared_days`）
   - [x] Overview 跨任务统计补强：签到判断改为按任务真实“坚持天数”属性识别，不再硬编码 `attr_id=1`
   - [x] 落实`design1.md`第一阶段：属性周期 `schedule_config`（daily/specific_days/shared_days）标准化落入 `calc_config`
   - [x] Records 过滤逻辑升级：仅渲染当前日期生效且未过期属性（按 weekday + 生效区间）
   - [x] Records 共享目标进度：`shared_days` 按同周共享日累计进度（如周六周日共享 50）
   - [x] 属性创建表单改为“渐进式明细”：默认简版 + 高级周期设置折叠面板 + 周期目标覆盖
   - [x] 共享目标属性展示优化：Records 卡片与属性列表增加共享 Badge 与提示
   - [x] Stats页面底部“周期专注数据”双视图图表开发（`update_dec5.md`：节律热力图 + 时间分配图 + Tab切换）
   - [x] Todos页面调优（`update_dec4.md`：限宽居中 + Inline Add + 未完成/已完成分段 + 删除按钮降噪）
   - [x] todo页面放在record界面之后
   - [x] time页面添加专注补记，点击弹出窗口填入任务（默认当前），日期（默认当前），开始时间，专注时长
   - [x] time页面也应该放一个时间线，用来替换掉时间记录组件，这个时间线使用任务主题颜色填充有专注记录的时间线，总览就显示今日所有的，背景画成带时间刻度的灰线（可以设置时间的起始和终止）
   - [x] 落实`update_dec3.md`
   - [x] 添加新的主题色： #2563EB #8B5CF6 #F59E0B #475569
   - [x] 明确了Stats与Records页面的设计语言`docs/运维文档索引.md`,并查看`update_dec2.md`进行第一次调优
   - [x] 查看`update_dec1.md`进行更新
   - [x] 趋势追踪图表的 X 轴重叠修复：X 轴 label 自动抽稀显示，避免重叠
   - [x] 雷达图空状态优化：新增虚线雷达底纹 + 引导文案 + “去配置目标属性”快捷入口
   - [x] 把热力图移到record界面
   - [x] 总览任务 Records 统计所有任务：展示各任务签到状态与当日完成度，热力图按“任意任务签到”计入
   - [x] 顶栏“更多”弹窗优化：贴近按钮显示，并在失去焦点时自动关闭
   - [x] 创建任务时的颜色选取多给几个具有设计感的颜色预选项，点击自定义再弹出调色盘
   - [x] 界面应该根据当前的任务颜色属性调制颜色
   - [x] 热力图强度按完成度分级渲染：有签到时至少绘制最浅色，完成度越高颜色越深
   - [x] records更新 
   - [x] Records 第一阶段重构：属性管理移入右上角“配置”抽屉，主界面聚焦每日签到
   - [x] Records 第二阶段重构：已签到状态切换为“今日成就总结卡片”，隐藏输入表单与保存按钮
   - [x] Records 第三阶段重构：新增趋势图（7/30天）、目标雷达图、年度打卡热力图
   - [x] 属性删除交互优化：桌面端 Hover 显示，移动端改为“更多”菜单操作
   - [x] 图表交互细节优化：趋势图/雷达图/热力图切换添加淡入过渡
   - [x] 顶栏危险操作降级：移除大面积“删除任务”按钮，收纳到“更多”菜单并增加二次确认
   - [x] 任务属性添加删除功能
   - [x] 添加默认属性坚持天数，添加签到按钮签到坚持天数 +1
   - [x] 签到时可记录任务属性今日数据；Records 页面签到后仅只读展示
   - [x] 按照原数据库结构属性可选添加目标值；`target_value <= 0` 统一视为无目标（落库 `-1`）
   - [x] 按原数据库结构属性在添加目标值时可设定权重
   - [x] 若任务存在目标属性，统计新增今日完成度（按权重加权计算）
   - [x] 错误信息弹窗优化：支持自动消失（3.5s）与手动关闭，不再依赖手动刷新
   - [x] 补齐《页面合并与重设计计划》文档（`docs/WebMVP页面合并计划.md`）
   - [x] Tasks 页面并入 Overview：Task Hub 统一任务切换/删除，新增任务改为弹窗入口
   - [x] 桌面端布局升级为 12 栏高密度网格（`xl/2xl`），降低大屏冗余留白与跳转成本
   - [x] Overview 组件化拆分：`TaskHubCard` / `CurrentTaskSnapshotCard` / `QuickActionsCard` / `CreateTaskModal`
   - [x] `Todos / Records / Time` 页签组件化拆分：`TodosTab` / `RecordsTab` / `TimeTab`
   - [x] `App.tsx` 降复杂度：页签逻辑改为 feature 组件组合，保留状态与用例编排
   - [x] 最小冒烟校验：`npm run build` + `npm run test -- --run` 全通过（24 tests）
   - [x] 记录展示优化：Records 页面按所选日期展示“已记录值”
   - [x] Overview Action Center 重构：移除“完成首个未完成待办”和右侧待办预览列表，避免与左侧主待办列表重复
   - [x] Overview 布局改为两栏：左侧数据概览+待办列表，右侧 sticky Action Center（窄屏优先显示 Action Center）
   - [x] 打通 `listRecords` 仓储接口：`httpRepository` / `sqliteLocalRepository` / `tauriSqliteRepository`
   - [x] 桌面壳 Rust bridge 新增 `list_daily_records` command 并通过 `cargo check`
   - [x] 最小冒烟校验更新：`npm run build` + `npm run test -- --run` + `pytest -q` 全通过

4. 2026.4.15
   - [x] 新建任务单独放在一个界面中
   - [x] 总览（task）统计所有任务的todo列表
   - [x] 添加固有属性专注时长（与待办类似，任务新建自带），底层记录秒，显示为 `xh ym`，若小时为0仅显示 `ym`
   - [x] 所有任务的专注时长属性在总览（task）统计
   - [x] 增加 Time 页面（正计时/倒计时，倒计时可设置，结束后记录到当前任务）
   - [x] 更新主目录文档（README/UPDATE），补充启动模式与 Web 超时排查说明
   - [x] Overview 新增 Quick Actions（快速新增待办 / 开始专注 / 写今日记录）
   - [x] 任务作用域刷新增强：待办/专注/记录操作后即时刷新 Overview/Todos/Time/Stats
   - [x] API 错误提示标准化（移除笼统 `Unknown API error`）
   - [x] UI 组件层增强：Button/Card/StatWidget/SegmentedTabs 统一视觉与类型协议
