# Docker 部署手册

## 目的
用于容器化部署前后端，适合服务器验证或团队联调。

## 基本步骤
1. 准备 `.env`（端口、API 地址等）
2. 执行 `docker compose up -d --build`
3. 检查健康状态与日志

## 常见问题
- 前端请求不到后端：检查 `VITE_API_BASE_URL` 是否正确。
- 后端健康检查失败：确认 `18765` 端口未冲突。
