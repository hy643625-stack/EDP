# 发布与同步

## 发布前检查
1. 更新 `CHANGELOG.md`
2. 执行前端构建：`npm run build`
3. 执行后端测试：`pytest backend/tests -q`
4. 生成安装包：`.\build_windows_installer.ps1`

## 交付物
- 安装包目录：`dist/installer/`
- 桌面可执行目录：`dist/EveryDayPerfect/`

## 同步规则
- 发给用户的安装包版本号必须与 `CHANGELOG.md` 一致。
- 每次发版都要保留一份发布记录（日期、版本、修复点、验证结果）。
